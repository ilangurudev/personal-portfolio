---
import PhotoLayout from '../../../layouts/PhotoLayout.astro';
import { getCollection, getEntry } from 'astro:content';
import { getAlbumPhotosWithExif } from '../../../utils/photo-helpers';
import PhotoLightbox from '../../../components/photo/PhotoLightbox.astro';
import PhotoIcons from '../../../components/photo/PhotoIcons.astro';
import { PhotoGallery } from '../../../components/react/PhotoGallery';

export async function getStaticPaths() {
  const albums = await getCollection('albums');
  return albums.map((album) => ({
    params: { slug: album.slug },
  }));
}

const { slug } = Astro.params;
const album = await getEntry('albums', slug!);
const photos = await getAlbumPhotosWithExif(slug!);
const sortedPhotos = photos.sort((a, b) => 
  b.data.date.getTime() - a.data.date.getTime()
);

const galleryPhotos = sortedPhotos.map(p => ({
  ...p,
  data: {
    ...p.data,
    date: p.data.date.toISOString()
  }
}));
---

<PhotoLayout title={album.data.title} description={album.data.description}>
  <PhotoIcons />
  <div class="album-page">
    <div class="album-header">
      <h1 class="album-title">{album.data.title}</h1>
      <p class="album-description">{album.data.description}</p>
      <time class="album-date" datetime={album.data.date.toISOString()}>
        {album.data.date.toLocaleDateString('en-US', { 
          year: 'numeric', 
          month: 'long', 
          day: 'numeric' 
        })}
      </time>
    </div>

    {sortedPhotos.length === 0 ? (
      <p class="no-photos">No photos in this album yet.</p>
    ) : (
      <>
        <div class="gallery-container" style="height: calc(100vh - 400px); min-height: 600px; flex: 1;">
          <PhotoGallery client:load photos={galleryPhotos} />
        </div>
        <PhotoLightbox photos={sortedPhotos.map(p => ({
          ...p,
          data: { ...p.data, albumTitle: album.data.title }
        }))} />
      </>
    )}
  </div>

  <style>
    .album-page {
      max-width: 1400px;
      margin: 0 auto;
      padding: 2rem 1.5rem;
      /* Ensure full height for virtual scrolling if we used height: 100% in gallery */
      min-height: 100vh;
      display: flex;
      flex-direction: column;
    }

    .album-header {
      text-align: center;
      margin-bottom: 2rem; /* Reduced from 4rem to give more space to gallery */
      padding-bottom: 2rem;
      border-bottom: 1px solid rgba(28, 25, 23, 0.1);
      flex-shrink: 0;
    }

    .album-title {
      font-family: 'Crimson Text', serif;
      font-size: 3.5rem;
      font-weight: 700;
      color: var(--charcoal);
      margin: 0 0 1rem;
      letter-spacing: -0.02em;
    }

    .album-description {
      font-size: 1.25rem;
      color: var(--gray);
      line-height: 1.6;
      margin: 0 0 1rem;
      max-width: 600px;
      margin-left: auto;
      margin-right: auto;
    }

    .album-date {
      color: var(--amber);
      font-size: 1rem;
      font-weight: 500;
    }

    .no-photos {
      text-align: center;
      color: var(--gray);
      font-size: 1.125rem;
      padding: 4rem 2rem;
    }

    .gallery-container {
      margin-top: 2rem;
    }

    /* Global styles for React-rendered Photo Cards */
    :global(.photo-card) {
      display: flex;
      flex-direction: column;
      text-decoration: none;
      color: inherit;
      transition: transform 0.2s ease, box-shadow 0.2s ease;
      border-radius: 4px;
      overflow: hidden;
      background: var(--cream-dark);
      cursor: crosshair;
      position: absolute; /* React Window positions absolutely */
    }

    :global(.photo-card:hover) {
      transform: translateY(-4px);
      box-shadow: 0 8px 24px rgba(0, 0, 0, 0.1);
      z-index: 10; /* Ensure hovered item is above */
    }

    :global(.photo-image) {
      width: 100%;
      height: 100%;
      overflow: hidden;
      background: var(--cream);
      position: relative;
    }

    :global(.photo-image img) {
      width: 100%;
      height: 100%;
      object-fit: cover;
      transition: transform 0.3s ease;
    }

    :global(.photo-card:hover .photo-image img) {
      transform: scale(1.05);
    }

    :global(.viewfinder-overlay) {
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      display: flex;
      align-items: center;
      justify-content: center;
      pointer-events: none;
      opacity: 0;
      transition: opacity 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    }

    :global(.photo-card:hover .viewfinder-overlay) {
      opacity: 1;
    }

    :global(.viewfinder-corners) {
      width: 85%;
      height: 85%;
      color: rgba(255, 255, 255, 0.85);
      animation: viewfinderFocus 0.3s cubic-bezier(0.4, 0, 0.2, 1);
      transition: color 0.2s ease, transform 0.2s cubic-bezier(0.4, 0, 0.2, 1);
    }

    /* Focus lock animation state */
    :global(.viewfinder-corners.focus-locked) {
      color: #22c55e !important;
      transform: scale(0.95);
      opacity: 1 !important;
    }

    /* Ensure overlay is visible during focus lock */
    :global(.photo-card .viewfinder-overlay:has(.focus-locked)) {
      opacity: 1 !important;
    }

    @keyframes viewfinderFocus {
      from {
        opacity: 0;
        transform: scale(1.1);
      }
      to {
        opacity: 1;
        transform: scale(1);
      }
    }


    @media (max-width: 768px) {
      .album-title {
        font-size: 2.5rem;
      }

      .album-description {
        font-size: 1.125rem;
      }
    }
  </style>
</PhotoLayout>
